/**
 * User Hooks
 *
 * This module exports all custom hooks for the user feature.
 */

// Re-export hooks from the centralized roles module
export { useRoles, useUserManagement } from '@/features/roles/hooks';
