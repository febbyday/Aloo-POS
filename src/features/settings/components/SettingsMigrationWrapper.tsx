import React from 'react';
import SettingsMigrationPage from '../pages/SettingsMigrationPage';

/**
 * Wrapper component for the Settings Migration page
 * This follows the pattern used by other settings components
 */
const SettingsMigrationWrapper: React.FC = () => {
  return <SettingsMigrationPage />;
};

export default SettingsMigrationWrapper;
